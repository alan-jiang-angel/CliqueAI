# Store { Fingerprint: MaxClique } to Redis !!!
# Loop through all input graphs and result clique files, calculate fingerprint,
# and store the max clique found for each unique fingerprint into Redis DB.
